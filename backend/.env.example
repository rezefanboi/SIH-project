# Copy this file to .env and fill in real values for your environment.
# Never commit an actual .env file.

# Path to the SQLite database file inside the container / volume.
DB_PATH=/data/register.db

# Port the Flask/gunicorn server listens on.
PORT=5000

# Set to 1 only for local development to enable Flask's debug/reload mode.
FLASK_DEBUG=0
